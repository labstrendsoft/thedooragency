import { BluedeathView } from '@/modules/Proyectos/BluedeathView';

export default async function Bluedeath() {
  // await new Promise((resolve) => setTimeout(resolve, 4000));

  return <BluedeathView />;
}
