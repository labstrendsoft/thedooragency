import { TopcombatView } from '@/modules/Proyectos/TopcombatView';

export default async function Topcombat() {
  // await new Promise((resolve) => setTimeout(resolve, 4000));

  return <TopcombatView />;
}
